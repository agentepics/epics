---
enabled: true
type: prompt
timeout: 120
---

The planning epic has become blocked. Log the blocker details. Planning should rarely be blocked -- if it is, the blocker likely affects the whole portfolio. Escalate per policy.yml and note the block in runtime/state.json.
