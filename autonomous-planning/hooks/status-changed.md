---
enabled: true
type: prompt
timeout: 180
---

An Epic's status has changed in the portfolio. Update the epic_portfolio in runtime/state.json to reflect the change. If an Epic was marked complete or abandoned, check if its objective still needs coverage and flag the gap. If a new Epic was activated, map it to its objective. Log the portfolio change in runtime/log/.
